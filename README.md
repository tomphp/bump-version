# Bump Version

A tool which updates files in your project with the current version number.

## Hello World

Running:

```shell, script()
bump-version
```

Will output:

```text, verify()
Hello, world!
```

